## 一、基础信息

- 平台：仅支持 Linux
- TUI 框架：ratatui
- 依赖外部命令：`fd`（文件名搜索）、`rg`（内容搜索）、`bat`（预览）

---

## 二、核心功能

### 1. 搜索模式

- 使用 `Tab` 键切换：
  - 文件名搜索（基于 `fd`）
  - 文件内容搜索（基于 `rg`）
- 输入查询后按 `Enter` 触发搜索（输入过程中不即时搜索）
- 搜索进行中再按 `Enter` 或 `Tab`：**先 kill 掉旧的 fd/rg 进程，再立即重新搜索/切换模式**（不再阻塞等待，见「搜索取消」）
- 输入框支持多行：`Shift + Enter` 插入真实换行，或直接输入 `\n`；两者都解析为真正的换行显示（而非字面 `\n`）
- **所有输入框（搜索框与各编辑弹窗）均按宽度自动折行**，长内容不会溢出或截断

---

### 2. 搜索路径

- 使用 `Ctrl + P` 弹出路径输入框，直接编辑搜索路径（支持 `~` 展开）

- 默认路径为：

  ```
  启动程序时的当前目录（cwd）
  ```

---

### 3. 文件名搜索（fd）

- 基于 `fd` 实现
- 匹配规则示例：

输入：

```
test.sh
```

匹配：

- `xxxxtest.sh`
- `test.shxxxx`
- `xxxtest.shxxxx`
- `tesh.sh`

其他情况不做匹配

- 文件列表建索引后缓存 60 秒，期间改关键词只在内存中过滤，不重复拉起 fd
- 同样会跳过「默认忽略目录」（见下文），避免扫描 `/proc` 等
- **与内容搜索保持一致：用户额外忽略目录（`Ctrl + I`）与文件大小上限（`Ctrl + S`）同样作用于文件名搜索**（fd 用 `--exclude` 排除忽略目录、`--size -<N>b` 跳过超大文件）；修改这两项设置会清空文件列表缓存并立即重搜

---

### 4. 文件内容搜索（rg）

- 基于 `rg` 实现
- 搜索文件中的内容
- 结果**流式返回**（边搜边显示），最多展示 400 条
- 支持多行搜索：`Shift + Enter` 插入真实换行，或输入 `\n`（解码为换行）；含换行时自动启用 `--multiline`；`\\` 表示字面反斜杠
- 精准匹配：`--fixed-strings` + 大小写敏感（不拆词、不当正则）

#### 默认忽略目录（写死固定，不可通过界面关闭）

rg / fd 搜索时始终忽略以下目录（当搜索根包含它们时自动排除），避免直接扫描 `/` 时陷入虚拟/庞大目录：

```
/proc
/sys
/dev
/run
/tmp
/var/tmp
/var/cache
/mnt
/media
/var/lib
/snap
.git
```

> 说明：不再做「Permission denied 目录缓存」之类的动态忽略；统一改为上面这份固定清单 + 用户自定义忽略目录（见 `Ctrl + I`）。

#### rg 性能参数组合

内容搜索固定使用以下参数：

- `-j 0`：线程数自动（= CPU 核数）
- `--no-messages`：抑制 stderr 的权限等错误，减少干扰
- `--hidden`：包含隐藏文件（配合 `!.git` 排除 `.git`）
- `--max-filesize <字节>`：跳过超大文件（默认 10M，可用 `Ctrl + S` 修改）
- 二进制文件：rg 默认即跳过（rg 无 grep 的 `--binary-files` 选项，等价于「二进制直接忽略」）

#### 优先返回常用目录（体验优化）

结果排序在「命中次数」之后引入**路径优先级**作为次级排序键：

```
优先（0）：/home  /etc  /usr/local
延后（2）：/usr   /var
其他（1）：其余目录
```

命中次数相同时，常用目录的结果排在前面，用户会感觉「秒出结果」（`/usr/local` 优先于 `/usr`，按最长前缀匹配）。

---

### 5. 文件列表操作

- 使用 `Alt + J` / `Alt + K` 选择文件（`Alt + J` 下一个、`Alt + K` 上一个）
- 当前选中文件需要高亮

---

### 6. 文件预览（bat）

- 选中文件后使用 `bat` 显示文件内容
- 图片文件不支持预览，仅显示提示
- 大文件只渲染头部（512KB），截断时显示提示
- 支持滚动：
  - `Ctrl + J`：向下滚动
  - `Ctrl + K`：向上滚动

---

### 7. 打开文件

- 按 `Ctrl + G` 打开选中文件
- 使用系统默认编辑器（如 `vim` / `nvim` / `vscode`）
- 若权限不足：
  - 使用 `sudo` 获取权限后打开

---

### 8. 忽略目录（Ctrl + I）

- **对文件名搜索（fd）与内容搜索（rg）同样生效**
- 按 `Ctrl + I` 弹出输入框，在「默认忽略目录」之外**额外**增加忽略目录
- 多个目录用**换行**分隔（`Shift + Enter` 插入真实换行，或直接输入 `\n`；不再使用英文逗号）
- 逐个校验路径真实性：必须是真实存在的目录，否则弹窗内报错且不关闭
- 空输入 = 清空额外忽略目录
- 设置会写入配置文件持久化（见「配置文件」），下次启动自动加载
- 若有查询词，确认后立即重新搜索生效（文件名搜索会同时清空文件列表缓存）

---

### 9. 文件大小上限（Ctrl + S）

- **对文件名搜索（fd）与内容搜索（rg）同样生效**
- 按 `Ctrl + S` 弹出输入框，修改单文件大小上限（rg 用 `--max-filesize`、fd 用 `--size -<N>b`）
- 默认 `10`（单位 M）
- 输入框**只允许输入整数或小数**（如 `10` 或 `0.2`），其他字符直接被拒绝
- 校验：必须为大于 0 的数（至多一个小数点）
- 内部按 `MB × 1024 × 1024` 换算为整数字节传给 rg / fd（两者都不接受小数单位）
- 设置会写入配置文件持久化（见「配置文件」），下次启动自动加载
- 若有查询词，确认后立即重新搜索生效（文件名搜索会同时清空文件列表缓存）

---

### 10. 复制文件路径（Ctrl + C）

- 按 `Ctrl + C` 把**当前选中文件的完整路径**复制到系统剪贴板
- **Ctrl + C 不再退出/取消程序**（退出与取消搜索统一用 `Esc`）
- 依次尝试外部剪贴板工具：`wl-copy`（Wayland）→ `xclip` → `xsel`（X11），用第一个可用且成功的
- 写入走子进程 stdin，不引入额外依赖；这些工具写入后会 daemonize，前台立即返回，不阻塞 UI
- 无选中文件 / 复制失败时以 toast 提示原因；成功提示“已复制文件路径到剪贴板”

---

## 三、性能与架构

### 1. 完全异步（UI 绝不阻塞）

```
UI 线程（ratatui，只渲染/处理按键）
        ↑ channel（mpsc，按代号 gen 过滤过期消息）
后台线程（读取子进程 stdout）
        ↑
   fd / rg 子进程
```

- UI 线程只负责渲染与按键，任何搜索都在后台线程以子进程方式运行
- 取消搜索时 UI 线程只做「置取消标志 + kill 子进程」，**不 join 后台线程**，因此不会卡顿；
  后台线程因管道 EOF 自行退出，其携带旧代号的消息会被 UI 丢弃

### 2. 流式搜索（Streaming）

```
rg 输出一行 → 解析 → 分批回传 → UI 立即显示
```

而非「等 rg 跑完再一次性显示」。

### 3. 背压控制（Backpressure）

避免疯狂往 UI 推数据导致界面卡死，采用两个维度（任一达到即刷新一次 UI）：

- 累计 **100 条**结果
- 间隔 **16ms（≈60fps）**

主循环以 16ms 节拍轮询事件并重绘。

### 4. 搜索取消（必须有）

- 用户输入新关键词后按 `Enter`：
  ```
  旧 rg/fd 进程 → kill → 重新搜索新关键词
  ```
- 搜索进行中按 `Esc`：
  ```
  旧 rg/fd 进程 → kill（仅取消当前搜索，不退出程序）
  ```
- **没有**搜索任务时按 `Esc`：退出 sift 程序
- `Ctrl + C` 不再用于取消/退出，改为复制选中文件路径（见「复制文件路径」）
- 结果达到上限（400 条）时也会主动 kill 后台 rg，提前结束

---

## 四、配置文件

- 路径：

  ```
  ~/.config/sift/config.toml
  ```

- 文件缺失或损坏时回退到默认值，不阻断启动；保存时自动创建父目录
- 字段格式：

  ```toml
  # 单文件大小上限（MB，默认 10.0；fd/rg 同样生效）
  max_file_size_mb = 10.0

  # 额外忽略目录（绝对路径，不含内置必忽略目录；Ctrl+I 编辑）
  ignore_dirs = ["/data/logs", "/backup"]
  ```

---

## 五、界面布局

```
┌──────────────────────────────────────────────┐
│ 搜索输入框                                   │
├───────────────┬──────────────────────────────┤
│ 文件列表       │ 文件内容预览（bat）           │
│               │                              │
│               │                              │
│               ├──────────────────────────────┤
│               │ 文件完整路径（自动换行）       │
├───────────────┴──────────────────────────────┤
│ 快捷键提示                                   │
└──────────────────────────────────────────────┘
```

---

## 六、界面要求

### 1. 顶部

- 搜索输入框
- 支持展开/折叠（`Ctrl + H` 切换，默认展开）：
  - **展开**：按宽度自动折行，高度最大为页面高度的 1/3；内容超过可视区时右侧出现滚动条，并随光标上下滚动查看全部内容；`↑ / ↓` 按折行上下移动光标（尽量保持原列）
  - **折叠**：单行高度，换行以 `⏎` 标记，超出宽度的部分以 `…` 截断
  - 标题栏左侧始终显示状态标识（Nerd Font 图标 + 文字）：展开 = `nf-oct-unfold`（U+F42D）+ “展开”（绿），收起 = `nf-oct-fold`（U+F48C）+ “收起”（橙）；终端需使用 Nerd Font 字体

### 2. 中部左侧

- 搜索结果文件列表

### 3. 中部右侧

- 上半部分：文件内容（bat 渲染）
- 下半部分：文件完整路径（支持折行）

### 4. 底部

- 快捷键提示栏

### 5. 配色与样式

- 配色方案：Catppuccin Macchiato
- 所有面板边框使用圆角
- 选中行为整行背景高亮，不使用箭头符号
- 文件预览使用 bat 的 Catppuccin Macchiato 主题（若可用）

### 6. 临时提示弹框（toast）

- 取消搜索（「已取消当前搜索」）、打开编辑器结果、配置保存失败等临时消息，以**居中弹框**展示
- 弹框宽度贴合消息文本（超长自动换行加高），停留 5 秒后自动消失
- 不占用底部快捷键提示栏（底栏太窄，放不下完整消息）

---

## 七、快捷键汇总

| 按键       | 功能                                   |
| -------- | ------------------------------------ |
| Tab      | 切换搜索模式（搜索中则先取消旧搜索再切换）           |
| Ctrl + H | 展开/折叠顶部搜索输入框（展开最高 1/3 屏可滚动；折叠单行截断） |
| Ctrl + P | 修改搜索路径（输入框）                          |
| Ctrl + I | 编辑额外忽略目录（换行分隔，持久化；fd/rg 同样生效） |
| Ctrl + S | 修改文件大小上限（M，持久化；fd/rg 同样生效） |
| Shift + Enter | 输入框插入换行（搜索框 / 忽略目录多行输入） |
| Alt+J / Alt+K | 选择文件（下一个 / 上一个）           |
| ↑ / ↓    | 展开态搜索框中按折行上下移动光标（折叠态无效） |
| Ctrl + J | 预览向下滚动（3 行）                          |
| Ctrl + K | 预览向上滚动（3 行）                          |
| PageDown / PageUp | 预览向下/向上滚动（10 行）              |
| Enter    | 触发搜索（搜索中则先 kill 旧进程再搜新关键词）           |
| Ctrl + G | 打开文件                                 |
| Ctrl + C | 复制选中文件的完整路径到剪贴板             |
| Esc      | 搜索中：取消当前搜索；非搜索：退出程序            |

---
