## 一、基础信息

- 平台：仅支持 Linux
- TUI 框架：ratatui

---

## 二、核心功能

### 1. 搜索模式

- 使用 `Tab` 键切换：
  - 文件名搜索（基于 `fd`）
  - 文件内容搜索（基于 `rg`）
- 输入查询后按 `Enter` 触发搜索（输入过程中不即时搜索）
- 搜索进行中再按 `Enter` 不会并发触发新搜索，而是弹出提示框告知用户等待当前搜索完成（3 秒后自动消失）

---

### 2. 搜索路径

- 使用 `Ctrl + P` 弹出路径输入框，直接编辑搜索路径（支持 `~` 展开）

- 默认路径为：
  
  ```
  启动程序时的当前目录（cwd）
  ```

---

### 3. 文件名搜索（fd）

- 基于 `fd` 实现
- 匹配规则示例：

输入：

```
test.sh
```

匹配：

- `xxxxtest.sh`
- `test.shxxxx`
- `xxxtest.shxxxx`
- `tesh.sh`

其他情况不做匹配

---

### 4. 文件内容搜索（rg）

- 基于 `rg` 实现
- 搜索文件中的内容
- 结果流式返回（边搜边显示），最多展示 400 条
- 支持多行搜索：查询中的 `\n` 解码为换行并自动启用 `--multiline`；`\\` 表示字面反斜杠
- 权限不足（Permission denied）的文件/目录会被缓存并聚合为排除规则，后续搜索自动跳过（有效期 10 分钟）

---

### 5. 文件列表操作

- 使用 `↑ / ↓` 选择文件
- 当前选中文件需要高亮

---

### 6. 文件预览（bat）

- 选中文件后使用 `bat` 显示文件内容
- 大文件只渲染头部（512KB），截断时显示提示
- 支持滚动：
  - `Ctrl + J`：向下滚动
  - `Ctrl + K`：向上滚动

---

### 7. 打开文件

- 按 `Ctrl + G` 打开选中文件
- 使用系统默认编辑器（如 `vim` / `nvim` / `vscode`）
- 若权限不足：
  - 使用 `sudo` 获取权限后打开

---

## 三、界面布局

```
┌──────────────────────────────────────────────┐
│ 搜索输入框                                   │
├───────────────┬──────────────────────────────┤
│ 文件列表       │ 文件内容预览（bat）           │
│               │                              │
│               │                              │
│               ├──────────────────────────────┤
│               │ 文件完整路径（自动换行）       │
├───────────────┴──────────────────────────────┤
│ 快捷键提示                                   │
└──────────────────────────────────────────────┘
```

---

## 四、界面要求

### 1. 顶部

- 搜索输入框

### 2. 中部左侧

- 搜索结果文件列表

### 3. 中部右侧

- 上半部分：文件内容（bat 渲染）
- 下半部分：文件完整路径（支持折行）

### 4. 底部

- 快捷键提示栏

### 5. 配色与样式

- 配色方案：Catppuccin Macchiato
- 所有面板边框使用圆角
- 选中行为整行背景高亮，不使用箭头符号
- 文件预览使用 bat 的 Catppuccin Macchiato 主题（若可用）

---

## 五、快捷键汇总

| 按键       | 功能     |
| -------- | ------ |
| Tab      | 切换搜索模式 |
| Ctrl + P | 修改搜索路径（输入框） |
| ↑ / ↓    | 选择文件   |
| Ctrl + J | 预览向下滚动 |
| Ctrl + K | 预览向上滚动 |
| Enter    | 触发搜索   |
| Ctrl + G | 打开文件   |

---


