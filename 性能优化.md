- 架构调整

  完全异步（UI绝不能阻塞）

  你的结构应该是：

  ```
  UI线程（ratatui）   ↓异步任务（tokio）   ↓rg或fd 子进程
  ```

  👉 Rust结构：

  - UI线程：只负责渲染
  - tokio任务：负责读 stdout
  - channel：传输结果

- 流式搜索（Streaming）——最关键

  设计必须是：

  ```
  rg或fd输出一行 → 解析 → UI立即显示
  ```

  而不是：

  ```
  等待rg或fd跑完 → 一次性显示 ❌
  ```

- 背压控制（很多人忽略）

  如果你疯狂往 UI 推数据：

  👉 UI 会卡死 ❌

  ***

  解决方案：

  ```
  限制 UI 更新频率（例如 16ms / 60fps）批量更新（batch）
  ```

  另外结合限制结果

  ```
  100条结果 → 一次 UI 更新
  ```

  以上两个维度任何一个达到即可更新UI

- 首先先删除一个需求：之前做的哪些文件在rg内容时有error,把那些目录忽略掉做一个缓存的需求去掉吧，就是不要相关的缓存了，直接做到写死固定忽略的目录为以下这些。

  ❗ rg时不要直接扫 `/`

  必须默认忽略以下目录：

  ```
  /proc
  /sys
  /dev
  /run
  /tmp
  /var/tmp
  /var/cache
  /mnt
  /media
  /var/lib
  /snap
  .git
  ```

  - `--binary-files=without-match`\
    👉 明确告诉 rg：**二进制文件直接忽略**
  - `--max-filesize 10M`\
    👉 rg超大文件直接跳过（非常关键！）

- 使用 ripgrep 最强参数组合

  推荐使用以下参数

  - `-j 0` 👉 自动最大线程
  - `--no-messages` 👉 减少 stderr 干扰
  - `--hidden` 👉 包含隐藏文件

- 提前“分批返回”（体验关键）

  你可以做：

  ```
  优先返回：
  /home
  /etc
  /usr/local
  延后返回：
  /usr
  /var
  ...
  ```

  👉 用户会感觉“秒出结果”

- 搜索取消（必须有）

  - 用户输入新关键词，点击Enter：

  ```
  旧 rg或fd 进程 → kill ❗-> 重新搜索新关键词
  ```

  - 在搜索中时，按Ctrl+C或Esc取消当前搜索

  ```
  旧 rg或fd 进程 → kill ❗
  ```

  - 之前在没有搜索任务时按Ctrl+C或Esc时退出sift程序，这个保持不变

- 文件内容搜索文件大小限制

  ```
  rg --max-filesize 10M
  ```

- 另外在进行rg搜索文件内容时，增加一个类似于路径输入的输入框用来增加忽略的目录（除必须默认忽略目录之外），每个目录用英文逗号分隔，需要校验每个路径的真实性，快捷键为Ctrl+I。下次打开时记住上次的设置，所以需要在配置文件中记录此配置，具体字段格式你来决定

- 另外在进行rg搜索文件内容时，增加一个类似于路径输入的输入框用来更改最大文件大小限制，默认是10M,可以更改，单位为M，输入框只允许输入小数或整数比方说0.2或10，快捷键为Ctrl+S。下次打开时记住上次的设置，所以需要在配置文件中记录此配置，具体字段格式你来决定

- 配置文件路径为\~/.config/sift/config.toml

- 以上所有内容最后都要落实到AGENTS.md文件中
